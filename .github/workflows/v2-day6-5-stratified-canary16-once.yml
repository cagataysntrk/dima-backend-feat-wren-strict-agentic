name: v2-day6-5-stratified-canary16-once

"on":
  push:
    branches:
      - feat/ask-v2-mvp
    paths:
      - ".github/workflows/v2-day6-5-stratified-canary16-once.yml"

jobs:
  canary16:
    if: "github.event.head_commit.message == 'test(v2-day6.5): run stratified canary16'"
    runs-on: ubuntu-latest
    environment: DIMA_OPENROUTER_API_KEY
    timeout-minutes: 20
    env:
      TARGET_SHA: "f5ca942f83aaee0806d7119957c543ab17a59a37"
      DIMA_LLM_PROVIDER: "openrouter"
      DIMA_V2_RESEARCH_MANAGER_PROVIDER: "openrouter"
      DIMA_OPENROUTER_API_KEY: ${{ secrets.DIMA_OPENROUTER_API_KEY || secrets.OPENROUTER_API_KEY }}
      DIMA_VQR_EMBEDDER: "off"
      DIMA_SCHEDULER_ENABLED: "false"
      DIMA_INTERACTION_LOG: "false"
    steps:
      - name: Capture workflow identity
        shell: bash
        run: |
          echo "workflow_source_sha=$GITHUB_SHA" | tee /tmp/day65_canary16_receipt.txt
          echo "requested_code_sha=$TARGET_SHA" | tee -a /tmp/day65_canary16_receipt.txt

      - uses: actions/checkout@v4
        with:
          ref: ${{ env.TARGET_SHA }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/pyproject.toml

      - name: Install backend eval dependencies
        working-directory: backend
        run: pip install -e ".[dev]"

      - name: Verify exact tested SHA
        shell: bash
        run: |
          tested_sha="$(git rev-parse HEAD)"
          echo "tested_code_sha=$tested_sha" | tee -a /tmp/day65_canary16_receipt.txt
          test "$tested_sha" = "$TARGET_SHA"

      - name: Stratified 16-case live canary
        working-directory: backend
        shell: bash
        run: |
          python lab/v2_day6_5_manager_eval.py \
            --require-live \
            --workers 1 \
            --model "openai/gpt-5.6-sol" \
            --case-id d65-dev-001 \
            --case-id d65-dev-009 \
            --case-id d65-dev-013 \
            --case-id d65-dev-019 \
            --case-id d65-dev-026 \
            --case-id d65-dev-025 \
            --case-id d65-dev-037 \
            --case-id d65-dev-005 \
            --case-id d65-dev-034 \
            --case-id d65-dev-047 \
            --case-id d65-dev-055 \
            --case-id d65-dev-056 \
            --case-id d65-dev-063 \
            --case-id d65-dev-067 \
            --case-id d65-dev-073 \
            --case-id d65-dev-079

      - name: Upload canary measurement and receipt
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: v2-day6-5-stratified-canary16
          path: |
            backend/lab/reports/v2_day6_5_manager_eval.json
            /tmp/day65_canary16_receipt.txt
          retention-days: 30
