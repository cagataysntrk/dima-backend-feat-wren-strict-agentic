name: v2-day6-focused-once

"on":
  push:
    branches:
      - feat/ask-v2-mvp
    paths:
      - ".github/workflows/v2-day6-focused-once.yml"

jobs:
  day6-contract:
    if: "github.event.head_commit.message == 'test(v2-day6): run one-shot focused gate'"
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      DIMA_LLM_PROVIDER: "rule"
      DIMA_VQR_EMBEDDER: "off"
      DIMA_SCHEDULER_ENABLED: "false"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/pyproject.toml
      - name: Install backend test dependencies
        working-directory: backend
        run: pip install -e ".[dev]"
      - name: Day 6 focused provider-free contracts
        working-directory: backend
        run: pytest -q tests/test_v2_day6.py
