# DIMA FAST TRACK — DURUM

Status: LIVING AUTHORITY
Branch: `feat/dima-metabase-product-fast-track`

## CURRENT_STATE

CURRENT_HEAD:
`1823d3380ee8ce3f0d3a11de96a617d742c3c8f9`

CURRENT_BACKEND_GATE:
`FT-002B CLOSED / GREEN`

CURRENT_PRODUCT_GATE:
`FT-003 — OPEN / CORE GREEN / LIVE RED`

FT-UI-002:
`CLOSED / GREEN`

FT-003_CORE:
`GREEN`

FT-003_CORE_RUN:
`35770650589`

FT-003_LIVE:
`RED`

FT-003_LIVE_RED_RUN:
`35770581158`

FT-003_LIVE_RED_SHA:
`9afe2529e58a28f6ff0be72eb9686f44d50cd224`

FT-003_FRONTEND:
`NOT YET PROVEN`

FT-003_FINAL:
`OPEN`

TARGET:
`Dima-native Intelligence Workspace on Metabase Analytics Substrate`

SELECTED_RENDERING:
`OPTION_A — Dima-native chart/table`

## SEALED BASELINES

F0_GREEN_BASELINE:
`a7bf0ccac99fa757f06919d0a043fd24e134bba0`

F0A_TESTED_COMMIT:
`c8175ea2a05be79eeae7c61d8fb568c36fbe3773`

FT-002B_TESTED_COMMIT:
`6c845b3dfdde70fa54590cef58a7a26c7b3dc6e5`

FT-002B_WORKFLOW_RUN:
`35763124599`

FT-UI-002_TESTED_COMMIT:
`3f4a34356d7b32a78c3c18f1c96a1879253001a2`

FT-UI-002_WORKFLOW_RUN:
`35768067813`

FT-UI-002_ARTIFACT_DIGEST:
`sha256:2ecaec2ad248f5d22d12503fa3dad3dab46539b85f3999b7b404f556a03780b3`

PARENT_SNAPSHOT:
`352205f112fe735d8f80065c7d255d78905398b9`

LAST_GREEN:
`FT-003 deterministic core`

LAST_GREEN_RUN:
`35770650589`

OPEN_RED:
`FT-003 live pinned-Metabase sentinel`

OPEN_RED_RUN:
`35770581158`

OPEN_RED_STEP:
`Run real Fast Ask API sentinel`

## COMPLETED

- F0 branch isolation.
- F0A pinned OSS Metabase substrate.
- FT-002B Fast-owned typed Gateway.
- FT-UI-002 Dima-native rendering.
- zero-license core invariant.
- FT-003 pre-development review.
- FT-003 deterministic cognition/authority contracts.
- opaque resource handles.
- opaque field handles.
- deterministic temporal binding.
- deterministic portable query builder.
- deterministic evidence/answer core.
- Fast-only application.
- auth negative/positive core proof.
- Fast boundary static proof.
- compile/core/service proof.
- Wren/V2/V3 isolation proof in deterministic core workflow.

## CURRENT INVARIANTS

- writes only to Fast Track branch;
- Wren/V2/V3 semantic hot-path dependency = 0;
- silent fallback = 0;
- raw SQL = 0;
- joins = 0 for FT-003;
- METABASE_LICENSE_BUDGET = 0;
- paid Metabase feature required for core product = 0;
- full-app embed required = 0;
- modular SDK required in production = 0;
- native Metabase workspace required for user = 0;
- Dima product must run on Metabase OSS;
- Metabase frontend fork/source patch = 0;
- browser service/admin secret = 0;
- Metabase AI is not Dima research owner;
- Dima owns user-facing analyst UX.

## OPEN DEBT / RISKS

1. FT-003 live pinned-Metabase sentinel is RED and must be classified before patching.
2. COUNT/SUM/BREAKDOWN must match an independent DB oracle on current HEAD.
3. Duplicate canonical resource URI must not create false ambiguity.
4. Unknown field handle must fail closed with execution = 0.
5. Field portable-FK fallback usage must be visible in the live receipt.
6. REAL_MODEL_COGNITION is not yet certified unless a provider-backed sentinel is run.
7. Isolated `/fast-poc` is not yet proven against real `/fast/ask`.
8. FT-003 must not be sealed until desktop/mobile browser proof and failure receipts are closed.

## NEXT_EXACT_ACTION

1. Create `FT_003_LIVE_SENTINEL_RED_001.md`.
2. Inspect run `35770581158` exact traceback/output.
3. Classify the RED before code changes.
4. Verify or reject the Decimal display-oracle hypothesis.
5. If EVAL_ORACLE, patch test oracle only; do not change product formatter.
6. Add duplicate-resource-URI and unknown-field-handle focused family tests.
7. Re-run live sentinel on current branch HEAD.
8. Seal live COUNT/SUM/BREAKDOWN receipt only when independent DB equality passes.
9. Run small real-model cognition sentinel if credentials/provider are available; otherwise record explicit debt.
10. Connect isolated `/fast-poc` to real `/fast/ask` and run browser E2E.
11. Seal FT-003 only after all hard gates are GREEN.

## FILES_NEXT_ALLOWED

- focused FT-003 failure receipts/docs;
- `backend/app/fast/**` only if evidence proves a product defect;
- `backend/tests/test_fast_ask*.py`;
- `.github/workflows/dima-fast-ft003-*.yml`;
- isolated `dima-frontend-demo-master/src/app/fast-poc/**`;
- `dima-frontend-demo-master/src/features/fast-poc/**`;
- focused Fast POC E2E;
- `backend/belgeler/fast/**`.

## FILES_NEXT_FORBIDDEN

- source branch writes;
- V2/V3/Wren modification;
- legacy Ask behavior modification;
- Analyst loop;
- Research manager;
- Root Cause;
- join support;
- arbitrary filters;
- multi-table support;
- generic Metabase workspace UI;
- paid Metabase embedding dependency;
- main frontend migration before FT-003 seal.
