# FT-005N — NATIVE METABOT SOURCE AUDIT

Status: SEALED / SOURCE FACTS CONFIRMED
Date: 2026-09-23

Source:
`metabase/metabase@v0.63.18`

Reference only. No source modification performed.

## 1. Native streaming endpoint exists

File:
`src/metabase/metabot/api.clj`

Blob SHA:
`91ab6c6c5f01e91dd15864dfba1da0e38abeb3f8`

Confirmed:
- POST `/api/metabot/agent-streaming`;
- request contains profile_id, message, context, conversation_id, history, state;
- request resolves Metabot/profile and checks Metabot enabled/access;
- native streaming path calls `agent/run-agent-loop`;
- stream persists/finalizes assistant turns;
- client disconnect is observed.

Result:
`AGENT_STREAMING_EXISTS = YES`

## 2. Native agent loop exists

File:
`src/metabase/metabot/agent/core.clj`

Blob SHA:
`468374a6ce771f165bbcd58b6d7c665e5e3fea74`

Confirmed:
- `run-agent-loop`;
- profile resolution;
- permission/scope enforcement;
- iterative loop;
- model call with current memory/context/tools;
- tool-call-driven continuation;
- max-iteration stop;
- terminal-tool stop;
- per-iteration memory update;
- query/chart extraction into memory/state;
- optional debug logging exists, but benchmark must not retain private reasoning.

Result:
`RUN_AGENT_LOOP = YES`

## 3. Profiles exist and are materially different

File:
`src/metabase/metabot/agent/profiles.clj`

Blob SHA:
`b71d664f07fea2722619fe1633469405ad966be1`

### nlq

Confirmed:
- max_iterations = 10;
- temperature = 0.3;
- retrieve_library_entities when available;
- fallback profile uses nlq_search;
- read_resource;
- construct_notebook_query;
- navigate_user;
- create_chart;
- edit_chart.

### internal

Confirmed:
- max_iterations = 10;
- search;
- construct_notebook_query;
- read_resource;
- create_sql_query;
- edit_sql_query;
- replace_sql_query;
- edit_chart;
- navigate_user;
- create_chart;
- create_autogenerated_dashboard;
- create_dashboard_subscription;
- analyze_chart.

Result:
`PROFILES = YES`
`NLQ_MAX_ITERATIONS = 10`
`INTERNAL_MAX_ITERATIONS = 10`

## 4. Tool registry exists

File:
`src/metabase/metabot/tools.clj`

Blob SHA:
`b5e445e4f29daaced3f5b0bd84734a25e09b093b`

Confirmed tool surface includes native analytics/search/resource/query/chart capabilities referenced by profiles.

Result:
`NATIVE_TOOL_SURFACE = YES`

## 5. Skills exist

File:
`src/metabase/metabot/skills.clj`

Blob SHA:
`859efb968255d99c26100919c9f263523bb1f927`

Confirmed:
- first-class skill registry;
- on-demand `load_skill` mechanism;
- skill/profile/tool associations;
- markdown task skills;
- SQL dialect skills;
- always-on vs on-demand skill behavior.

Result:
`SKILLS = YES`
`DIALECT_SKILLS = YES`
`LOAD_SKILL = YES`

## 6. Agent API shares Metabot primitive implementations

File:
`src/metabase/agent_api/api.clj`

Blob SHA:
`a85e54ca9252248c04f710193a99cdf0fd5dce4c`

Confirmed imports:
- `metabase.metabot.tools.construct`;
- `metabase.metabot.tools.resources`;
- `metabase.metabot.tools.search`.

Confirmed /v1/search delegates to `metabot-search/search`.

Confirmed construct path uses `metabot-construct/execute-representations-query`.

Result:
`AGENT_API_SHARES_METABOT_PRIMITIVES = YES`

Interpretation:
native-vs-Fast differences should first be attributed to orchestration/prompt/skills/memory/state/repair/context rather than automatically blaming a separate query engine.

## 7. Native frontend carries history/state

File:
`frontend/src/metabase/metabot/state/actions.ts`

Blob SHA:
`426050384e265e0f083dd192f1a089ee8226f31b`

Confirmed:
- frontend calls `/api/metabot/agent-streaming`;
- response `state` data parts update conversation state;
- result returns accumulated history + new response history;
- abort path preserves received history and latest received state;
- later submissions send current request metadata including history/state.

Result:
`NATIVE_HISTORY_STATE_CARRY_FORWARD = YES`

## 8. OSS frontend plugin default

File:
`frontend/src/metabase/plugins/oss/metabot.ts`

Blob SHA:
`b11269acd7b1f14646bc52ffbad261428895a133`

Confirmed:
`isEnabled = false` in OSS frontend plugin default.

This does NOT prove backend endpoint unavailable at runtime.

Required runtime classification:
- NATIVE_ENDPOINT_AVAILABLE;
or
- NATIVE_ENDPOINT_NOT_AVAILABLE_IN_THIS_STOCK_CONFIG.

No license/feature-gate patch is permitted.

## 9. Boyahane deployment control

Repository:
`cagataysntrk/metabase-boyahane`

Observed HEAD:
`f0c4a6b053ead52ca2eac80002c448323dc34a35`

Configured compose:
`metabase/metabase:latest`

Source data:
- 80 tables;
- 462,962 rows;
- PostgreSQL target;
- dedicated read-only Metabase database role.

A0 exact running image ID/RepoDigest/version/provider/model cannot be inferred safely from Git repository configuration alone and must be captured from the running environment without mutation.

## 10. Pinned A1 control

Required runtime:
`v0.63.18`

Required image digest:
`sha256:1160b570cb11c107bce00e71293552df8a8363e01a32c2c7a048cee002dc8a73`

Use an isolated compose/benchmark environment.

Do not change Boyahane main compose.

## 11. Hard conclusions

```text
v0.63.18 agent-streaming exists = YES
run-agent-loop = YES
profiles = YES
max iterations = YES
skills = YES
state/memory = YES
native search/read/construct = YES
Agent API shares Metabot primitive implementations = YES
OSS frontend Metabot plugin enabled by default = NO
stock backend endpoint runtime availability = TO BE MEASURED
license bypass = FORBIDDEN
```

## 12. Architecture hypothesis admitted for experiment

```text
primitive capability loss
= likely limited

orchestration seam capability loss
= plausible and material enough to measure
```

This is a hypothesis, not a verdict.

No Fast product code change is authorized by this source audit.
