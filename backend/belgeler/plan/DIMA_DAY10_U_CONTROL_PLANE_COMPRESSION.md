## LATEST AUTHORITY — SOURCE-TRUTH LIVE-PROVEN / HARNESS CAP FIXED / PAID RE-MEASUREMENT IN PROGRESS

```text
branch                           feat/ask-v2-mvp
engineering measurement base     a70a20997e86f87dd83baa40845e8eaa01b2ae49
Product behavior                 6dacfebd70caf0e91e2d701192d2de41fe4d66f6
backend/app diff after Product   ZERO

Day10 focused                    36227497718 = GREEN
Day8 focused                     36227497717 = GREEN
Day7 focused                     36227497720 = GREEN

harness correction Day10         36228073546 = GREEN
harness correction Day8          36228073620 = GREEN

source-truth revision family     FAM-PREACCEPTANCE-REVISION-SOURCE-TRUTH-DRIFT
source-truth status              CLOSED / LIVE-PROVEN
latest prior paid                36227806311 = FAILURE / NO PRODUCT VERDICT
prior terminal                   SEMANTIC_LINKER HARNESS CAP 3 / 3
harness semantic role cap        4
global paid hard cap             20
active family                    FAM-PAID-HARNESS-SEMANTIC-ROLE-CAP
active family status             FIXED / PAID RE-MEASUREMENT AUTHORIZED

current paid run                 36228701676 = IN PROGRESS
current paid epoch               0 / 40 used before run
current paid epoch remaining     40 / 40 before run
measurement cap                  20 calls

ManagerBudget                    4 / 4 / 8 = FEASIBLE / RATIFIED
ActionSet materializability      GREEN / RATIFIED
READY hydration parity           GREEN / RATIFIED
finding supersession provenance  GREEN
signed continuation architecture HEALTHY provider-free

Day8 FINAL                       SEALED
Day10 FINAL                      OPEN / PAID GATE RUNNING
DEV80                            NOT AUTHORIZED YET
```

Current source-truth invariant:

```text
FINITE PREACCEPTANCE REVISION
MAY CHANGE OBLIGATION STRUCTURE.

IT MUST NOT RANDOMLY CHANGE
THE CANONICAL MEANING OF THE SAME EXACT USER SOURCE.

SOURCE TRUTH LIFETIME
MUST OUTLIVE DRAFT REVISION
WITHIN ONE IMMUTABLE USER TURN.

OWNER AUTHORITY MAY BE REMINTED.
SOURCE TRUTH MUST NOT BE RE-ROLLED.

IDENTICAL NEGATIVE SEMANTIC QUESTION
+ IDENTICAL GOVERNED CANDIDATE UNIVERSE
= NO NEW COGNITION VALUE.

different governed candidate universe
= fresh bounded cognition may run
```

Current generic continuation fix:

```text
SIGNED CONTINUATION
DOES NOT CREATE ANALYTICAL AUTHORITY.

signed section
→ server derives section-local inherited analytical authority
→ model selects bounded policy intent
→ server hydrates exact canonical parent
→ IntentAcceptanceGate validates active lineage/version/section admission
→ runtime executes existing governed lifecycle
```

Permanent continuation invariants:

```text
PRESENTATION AUTHORITY != ANALYTICAL AUTHORITY

ADAPT_ON_EVIDENCE
!=
BROADEN_WITHIN_BUDGET

ADAPT_ON_EVIDENCE
= inspected VERIFIED Evidence reveals MATERIAL_NEW_DIRECTION
  → completion-relevant follow/account lifecycle

BROADEN_WITHIN_BUDGET
= bounded permission for additional governed work when useful/necessary
  → authorization only
  → never invent work merely to close it

CANONICAL IDENTITY WIRING
= SERVER WORK
!= LLM COGNITION

signed token
= locator + integrity proof only
!= semantic / Evidence / canonical authority payload

0 or multiple admissible inherited analytical parents
→ fail closed / clarify
→ never choose by order, label, title, similarity, regex, or opaque ID echo
```

Protected owners remain unchanged:

```text
Wren
CrossDomainJoinGate
SemanticBindingGate
source-truth consistency
unique-scope determinism
Standard cube coherence
ManagerActionSet final-turn starvation correction
ManagerBudget 4 / 4 / 8
CompletionGate VERIFIED/PARTIAL truth
EvidenceStore
HypothesisLedger
EpistemicLabelGate
provider topology
```

Historical receipts below remain immutable audit history and do not override this physically first block.

## LATEST AUTHORITY — SOURCE-TRUTH CONSISTENCY + HONEST BLOCKED LIFECYCLE ACTIVE / PAID HARD STOP

```text
branch                           feat/ask-v2-mvp
engineering HEAD                 889fdbe6bc638ef8d2d6b6376decd32b8d67d84f
Product behavior                 fde87df46c3fdee986fdf48f4e37884f5b51c395
backend/app diff after Product   ZERO

D10-U PROVIDER-FREE              CLOSED / GREEN
one-SHA affected closure         dbf256f9122028fdb5398f360a951a66030a7fe5
Day7 focused                     36182712251 = GREEN
Day8 focused                     36182712368 = GREEN
Day10 focused                    36182712258 = GREEN
latest transport/focused         36183603679 = GREEN

unique-scope nondeterminism      CLOSED / LIVE-PROVEN
latest paid                      36183555931 = RED
accepted contract                YES
Wren cube_sql / dry_plan / query 4 / 4 / 4
relationship                     BLOCKED_DATA_GAP
ROOT                             VERIFIED
ADAPT directive                  OPEN
Product                          PARTIAL / BUDGET_EXHAUSTED

primary family                   FAM-CURRENT-TURN-SOURCE-TRUTH-DIVERGENCE
cycle                            1 / 3
secondary family                 FAM-BLOCKED-DIRECTIVE-TERMINAL-UNREACHABLE
cycle                            1 / 3

PAID_EPOCH_2                     29 / 40 used
PAID_EPOCH_2 remaining           11 / 40
paid execution                   STOP / INSUFFICIENT FULL ENVELOPE
max scientifically valid run     20 calls; cap MUST NOT be lowered

Day8 FINAL                       SEALED
Day10 FINAL                      OPEN
Day11 / DEV80 / Validation50 /
Hidden50                         NOT AUTHORIZED
```

Current engineering order:

```text
1. SOURCE-TRUTH CONSISTENCY
2. semantic + real-Wren relationship proof
3. BLOCKED directive lifecycle reconciliation
4. blocked lifecycle + certification proof
5. same-final-Product-SHA Day7 / Day8 / Day10 cheap closure
6. STOP at FINAL PROVIDER-FREE CANDIDATE
```

Permanent new invariants:

```text
SOURCE TRUTH != OWNER AUTHORITY

same exact current-message source_ref + same semantic kind
→ one canonical source decision

multiple owner obligations
→ fresh parent-owned sem_* authority per owner

authoritative partial-terminal parent
→ dependent ADAPT directive reconciles BLOCKED server-side
→ honest PARTIAL, never cognition-until-budget-exhaustion
```

Paid must not resume until the owner supplies a fresh sufficient envelope.

Permanent determinism boundary:

```text
SERVER-DETERMINED UNIQUENESS
→ deterministic server materialization

GENUINE SEMANTIC AMBIGUITY
→ bounded cognition

SAME SHA + SAME AUTHORITY STATE
must not oscillate between ACCEPT and CLARIFY
because of bookkeeping-level scope selection.

RETRIEVAL != AUTHORITY
REPAIR GROUP != PERSISTED AUTHORITY
SemanticBindingGate remains the sem_* minter.
```

Historical receipts below remain immutable audit history and do not override this physically first block.

## LATEST AUTHORITY — D10-U PROVIDER-FREE GREEN / PAID PREACCEPTANCE RED / SCOPE-REPAIR DETERMINISM ACTIVE

```text
branch                           feat/ask-v2-mvp
Product behavior tested          6fff55dc9ccfb279ae1352c8df7c39f0fbb275c2

D10-U PROVIDER-FREE CLOSURE      GREEN / CLOSED
final one-SHA cheap proof        a0b1be2e7b80e5928fa108a82c143f8002ee9c71
Day7 focused                     36178896617 = GREEN
Day8 focused                     36178896568 = GREEN
Day10 focused                    36178896533 = GREEN

latest paid                      36179726900 = RED
failure stage                    PREACCEPTANCE
Product lane/status              RESEARCH / CLARIFY
accepted contract                NULL
CompletionGate                   NEEDS_CLARIFICATION
Wren cube_sql / dry_plan / query 0 / 0 / 0

failure family                   FAM-PREACCEPTANCE-SCOPE-REPAIR-NONDETERMINISM
cycle                            1 / 3
first wrong transition           single server-generated full-cover scope
                                 → stochastic repair ABSTAIN
                                 → CLARIFY
historical counterexample        36128949886
                                 same semantic shape
                                 → SELECT_SCOPE_GROUP
                                 → ACCEPTED

PAID_EPOCH_2                     18 / 40 used
PAID_EPOCH_2 remaining           22 / 40
paid execution                   PAUSED
same-SHA retry                   FORBIDDEN

Day8 FINAL                       SEALED
Day10 FINAL                      OPEN
Day11 / DEV80 / Validation50 /
Hidden50                         NOT AUTHORIZED
```

Current owner:

```text
ManagerSemanticResolutionAdapter
→ _resolve_decomposition_repair(...)
```

Permanent boundary:

```text
SERVER-DETERMINED UNIQUENESS
→ deterministic server materialization

GENUINE SEMANTIC AMBIGUITY
→ bounded cognition
```

D10-U control-plane architecture remains CLOSED/GREEN and must not be reopened.
Historical receipts below remain immutable audit history.

# DIMA DAY10-U — CONTROL-PLANE COMPRESSION

Date: 2026-09-25

Status:

```text
D10-U                              ACTIVE
branch                             feat/ask-v2-mvp
Day8 FINAL                         SEALED
Day10 FINAL                        OPEN
PAID_EPOCH_2                       12 / 40 used
paid engineering status            PAUSED
Day11 / DEV80 / Validation / Hidden NOT AUTHORIZED
```

## 1. Why D10-U exists

The post-acceptance Manager control plane accumulated several separately reconstructed
representations:

```text
authoritative Product state
→ ManagerActionAvailability
→ scope seeds
→ ActionApplicabilitySnapshot
→ scoped provider schema
→ scoped parser / matcher
→ runtime authority
```

Provider-free real-Wren proof showed the inverse of the earlier illegal-combination
failure: authoritative state made a legal ROOT action executable while a later
projection layer hid that legal action from the model.

This is failure family:

```text
FAM-CONTROL-PLANE-PROJECTION-DIVERGENCE
Cycle 1/3
```

The snapshot experiment from the stable checkpoint through the pre-D10-U control-plane
HEAD is retained in Git history as an engineering receipt. It is not sealed Product
authority.

## 2. Recovery decision

D10-U uses ordinary forward commits; history is not reset or rewritten.

The affected Product control-plane behavior was compressed back toward the last stable
pre-snapshot behavior `2b28f23e...`, while preserving:

- certification-oracle correction;
- Day8 FINAL seal;
- ManagerBudget 4/4/8;
- Wren execution truth;
- Evidence / QueryContract truth;
- Hypothesis / epistemic truth;
- CrossDomainJoinGate;
- tenant/principal gates;
- ResearchTask identity/idempotency;
- STANDARD / RESEARCH ownership.

Only the post-acceptance cognition control plane is being replaced.

## 3. Canonical architecture

Target and implementation:

```text
authoritative Product state
        ↓
ONE PURE ManagerActionSet
        ↓
action_instances[]
        ↓
provider schema generated directly from the same set
        ↓
model selects action_ref + cognitive fields only
        ↓
server hydrates canonical identity wiring
        ↓
recompute same ManagerActionSet
        ↓
stale / invented action_ref fails closed
        ↓
existing runtime authority gates
        ↓
execute
        ↓
Evidence / state
        ↓
recompute ManagerActionSet
```

Canonical Product owner:

```text
backend/app/v2/manager_action_set.py
```

`manager_action_availability.py` is no longer an independent truth owner. It is a
historical import shim only.

## 4. Model/server ownership

The model may choose genuine cognition:

- which materially different executable action instance;
- bounded hypothesis statement;
- SUPPORTS / CONTRADICTS;
- NO_MATERIAL_DIRECTION reason;
- bounded material reason;
- required operation parameter when it is genuinely not server-determined.

The server owns bookkeeping identity:

- parent obligation;
- Evidence ref;
- semantic-handle set;
- hypothesis identity;
- directive identity;
- task identity;
- relationship input wiring;
- state-version binding.

When several Evidence identities differ only by server bookkeeping, the current
eligible Evidence is selected server-side rather than exposed as opaque but
semantically indistinguishable choices.

## 5. Shared guards

```text
backend/app/v2/authority_invariants.py
```

owns the pure parent-lineage projection shared by Product and certification.
Certification does not maintain a second independent lineage implementation.

The principle is:

```text
same invariant
→ one pure implementation
```

Runtime authorities still revalidate independently as defense in depth.

## 6. Exact executable predicates

ActionInstance existence must match the real downstream materializer.

Examples already corrected during D10-U:

- adaptive directive disposition binds only eligible parent-lineage Evidence;
- branch proposal requires direct parent-owned inspected VERIFIED Evidence and a
  completed parent ResearchTask matching that Evidence;
- ROOT next-test requires not merely lineage-valid Evidence, but Evidence satisfying
  the exact ResearchTask materialization parent contract;
- semantic-handle inputs remain correlated with their parent/hypothesis/task;
- pending hypothesis relation binds the exact hypothesis with its pending post-test
  Evidence;
- ready ResearchTask execution is one server-bound action instance.

No runtime truth gate was weakened to make ActionSet pass.

## 7. Provider transport

Post-acceptance provider schema is generated directly from `ManagerActionSet`.

The provider does not receive canonical identity fields merely to echo them.

Conceptually:

```text
{
  choice: {
    action_ref: act_...
    <cognitive fields only>
  }
}
```

After cognition, the server resolves the action_ref and hydrates the canonical internal
ManagerDecisionTransport.

Before execution the current ActionSet is recomputed. If the old action_ref is absent,
execution fails closed as stale.

## 8. Fixture migration boundary

Product runtime contains no compatibility parser for historical fake-manager JSON.

Shared provider-free adapter:

```text
backend/lab/v2_manager_action_set_script_adapter.py
```

Test facade:

```text
backend/tests/helpers/manager_action_set_adapter.py
```

Old scripted intent may be expressed in fixtures, but the adapter converts it to the
current ActionSet contract before Product sees it.

This is failure family:

```text
FAM-SCRIPTED-MANAGER-TRANSPORT-MIGRATION
Cycle 1/3
```

and is an EVAL / FIXTURE / TRANSPORT migration, not Product semantic truth.

## 9. Family ledger

```text
FAM-ACTION-ARG-APPLICABILITY

Cycle 1
36128949886
foreign ROOT handle exposed

Cycle 2
36139969231
foreign directive Evidence exposed

status
historical safety lesson retained
```

```text
FAM-CONTROL-PLANE-PROJECTION-DIVERGENCE

Cycle 1
legal ROOT action disappeared between authoritative state and model-facing surface

D10-U manifestations found while closing the family:
- duplicated projection layers hid executable ROOT cognition;
- next-test ActionSet initially treated lineage-valid Evidence as equivalent to the
  stricter ResearchTask materialization Evidence contract;
- server bookkeeping Evidence variants initially remained model-visible as opaque but
  indistinguishable action choices.

status
ACTIVE until all D10-U gates are GREEN
```

```text
FAM-SCRIPTED-MANAGER-TRANSPORT-MIGRATION

Cycle 1
historical scripted managers emitted legacy ManagerDecisionTransport JSON

status
ACTIVE until Day7/Day8/Day10 fixtures are GREEN on current contract
```

```text
FAM-CERTIFICATION-TRAJECTORY-DRIFT
Cycle 1
CLOSED
```

## 10. Metabase architecture harvest

Reference:

```text
metabase/metabase
20f3f6cbdc93e08deb64e18d7ad2e8f174a78b77
```

Architecture only; no runtime/code port.

Harvested parallels:

```text
resolved profile
→ capability / scope filtering
→ actual tools
→ model

applicability
→ concrete materializable plan item
→ orchestrator

same guard
→ one shared implementation

tool attempted
!=
tool succeeded
```

Dima equivalent:

```text
authoritative state
→ ONE executable ManagerActionSet
→ model
→ deterministic materialization
→ runtime authority
```

## 11. Permanent lessons

```text
GOVERNANCE SAFETY
!=
GOVERNANCE STATE PROLIFERATION

THE CONTROL PLANE MUST CONSTRAIN THE AGENT,
NOT BECOME A SECOND AGENT.

ONE AUTHORITATIVE STATE
→ ONE EXECUTABLE ACTION PROJECTION

MODEL CHOOSES REASONING,
SERVER OWNS CANONICAL IDENTITY WIRING.

LEGAL ACTION MUST BE VISIBLE.
ILLEGAL ACTION MUST BE ABSENT.

RUNTIME REMAINS DEFENSE IN DEPTH.

TEST FIXTURES MUST FOLLOW PRODUCT CONTRACT;
PRODUCT MUST NOT CARRY TEST-COMPATIBILITY TRANSPORT.

FAILURE CYCLES ARE FAMILY-SCOPED.

FINAL CERTIFICATION
= AUTHORITY / LIFECYCLE TRUTH,
NOT HISTORICAL PATH TELEMETRY.
```

## 12. D10-U close gate

D10-U remains ACTIVE until all are true:

```text
one canonical ManagerActionSet owner
provider schema consumes it directly
runtime hydration consumes it directly
no independent applicability truth
no Product test-compatibility transport
legal action never disappears
illegal identity cross-product absent
stale/invented action_ref fails closed

Day7 affected                         GREEN
Day8 affected                         GREEN
Day10 focused                         GREEN
real-Wren ROOT                        GREEN
real-Wren relationship                GREEN
provider-free canonical rehearsal     GREEN
ManagerActionSet attacks              GREEN
certification oracle                  GREEN

test_d10_s_real_wren_state_aware_action_profile_reaches_report
                                      GREEN
```

Until then:

```text
PAID_EPOCH_2
12 / 40 used
28 / 40 remaining

paid
PAUSED
```

After all D10-U close gates are GREEN, exactly one corrected integrated
`CANONICAL_NS4` measurement with hard per-run cap 20 is authorized. No same-SHA
stochastic retry.
