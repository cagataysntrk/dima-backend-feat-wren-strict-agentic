# GLOBAL PLATFORM özellik varsayılanları (ADR-0009 fabrika katmanının EN DİBİ).
#
# "fabrika ayarı" = factory-default (koda gömülü varsayılan) — üretim tesisi DEĞİL.
# Buradaki bayraklar SEKTÖR/ŞİRKETTEN BAĞIMSIZ platform yetenekleridir; tek yerde durur,
# tüm şirketlerce miras alınır ve admin panelde HER tenant için keşfedilebilir olur.
# Sektör pack.yml artık yalnız SEKTÖRE-ÖZEL bayrak taşımalı (ör. OEE).
#
# Öncelik (soldan sağa artar): global(bu dosya) < sektör < şirket < DB-override
# (global-scope < sektör < tenant < rol < kullanıcı). En spesifik kazanır.
# NOT: buradaki "global" = platform-geneli fabrika varsayılanı; DB'deki "global" = global
# SCOPE override (ayrı kavram, aynı sözcük).

# Aşamalar: off | alpha | beta | prod. `beta` = herkese KAPALI değil ama varsayılanda
# yalnız burada tanımlı; admin panelden tenant/rol/kullanıcı override'ı ile açılır.
# Metadata (etiket/açıklama/kategori) app/features.py FLAG_REGISTRY'de — tek kaynak.
features:
  # 🔴 KÖK-1 FAZ 1 — NİYET İZİ. Varsayılan **`prod`** ve bu bir karar: Faz 1'in tamamı
  # GÖRÜNÜRLÜKTÜR ve hiçbir kararı değiştirmez (`test_kok1_niyet.py::
  # test_ROUTE_DAVRANISI_DEGISMEDI` bunu kilitler). Kapalı bir gözlem, olmayan bir
  # gözlemdir; ve Faz 2'nin tüketici göçü bu izin ürettiği ölçüme dayanacak.
  # ⚠ Değer `"on"` yazılmıştı ve bayrak kaydı kapısı reddetti: geçerli aşamalar
  # `off|alpha|beta|prod`. Kapı haklıydı — *bir bayrağın aşaması bir "açık/kapalı"
  # değil, bir OLGUNLUK beyanıdır.*
  # ⚠ GERİ AL tek satır: `"off"` → tek bir iz satırı bile eklenmez.
  niyet_izi: "prod"
  # FAZ 2.3 — kapsam merceği. `off`: her kullanıcı bugünkü TAM katalogu görür.
  # 🔴 Mercek bir GÖRÜNÜRLÜK aracıdır, GÜVENLİK SINIRI DEĞİL — kapatmak yetki AÇMAZ.
  kapsam_mercegi: "off"   # ⚠ TIRNAK ŞART: YAML 1.1 çıplak `off`u BOOLEAN okur
  # `§B10` SKILLS — metodoloji markdown'ları (`demo/skills/*.md`) garsonun katalog
  # metnine eklenir. `off`: tek bayt bile eklenmez (KURAL B).
  #
  # 🔴 AÇILIŞ ŞARTI (gözlenebilir): garsonun doğruluk ölçümü kurulmalı (`§26`). Kazancı
  # ölçemediğimiz bir bağlam eklemesini varsayılan açmak, `motor_rls`/`ossie_ithal` için
  # reddettiğimiz şeyin aynısı olurdu.
  # ⊙ Açıldığında ilk ölçülecek vaka yazılı: *«geçen yıla göre ciro büyüme oranı»*
  #   bugün CEVAPSIZ; skill'li hâlde `toplam_ciro_degisim_yuzde` dönmeli.
  skills: "off"
  # FAZ 3.4 — Ossie ithali. `off`: uç 404 döner (GERİ AL).
  #
  # 🔴 `§⑥` — **AÇILDI (2026-08-12), ÇÜNKÜ İKİ GEREKÇESİ DE ÖLÇÜMLE DÜŞTÜ.**
  #
  # Bayrak `off` tutuluyordu ve gerekçesi şuydu: *«o bir YAZMA yoludur ve gerçek bir
  # müşteri modeliyle pilotu YAPILMADI»*. İkisi de ölçüldü:
  #
  # | gerekçe | ölçülen |
  # |---|---|
  # | *«YAZMA yolu»* | ⊘ **YANLIŞ.** `ossie.cevir()` docstring'i: *«Saf fonksiyon — dosya yazmaz»*; `ossie.py`'de sıfır yazma ilkeli (`open`/`write_text`/`mkdir`/`yaml.dump` — hiçbiri) ve uç sonucu yalnız **döndürüyor**. İthal bir **çeviri önizlemesidir**, kalıcılaştırma ayrı bir yönetici eylemidir |
  # | *«gerçek modelle pilot yok»* | ⊘ **YANLIŞ.** `test_f6_ossie_canli_sekil.py::test_ROUND_TRIP_canli_katalogda_KAYIPSIZ` **canlı katalogda** `cevir`'i koşuyor ve yeşil |
  #
  # ⊙ Yani kapalılık, kardeşi (`ossie_ihrac`) için yeterli sayılan kanıtın **aynısı**
  # varken sürüyordu. *Bir yeteneği, blokajı ölçülmeden kapalı tutmak, kararı bir
  # alışkanlığa çevirir.*
  #
  # 🔴 **CANLI PİLOT (2026-08-12):** kendi canlı ihracımız (58.465 bayt · 23 dataset ·
  # 31 ilişki) ithal ucuna geri beslendi → aşağıdaki ölçüm.
  #
  # ⚠ **SINIR — açıkça yazılı:** uç dışarıdan **rasgele** bir belge alır ve `cevir`'de
  # boyut/sayı sınırı YOKTUR. Bu ucun getirdiği YENİ bir risk değildir (uç kimlik
  # doğrulamalıdır — `connection:read` + şirket — ve `/ask` dahil her uç aynı özelliği
  # taşır); ama bir gün genel bir istek-boyutu sınırı konursa **buraya da** uygulanmalı.
  #
  # 🔴 `prod` ŞARTI (gözlenebilir): canlı `POST /connections/{cid}/ossie` **200** dönmeli
  # ve gövdesinde `cubes` sayısı, beslenen `datasets` sayısına **EŞİT** olmalı.
  ossie_ithal: "beta"
  # FAZ 4.4 — Ossie ihracı. `off`: uç 404 döner. İhraç OKUMA işlemidir, `packs/` etkilenmez.
  #
  # 🔴 `§F6` — **AÇILDI (2026-08-11), ÇÜNKÜ BAYRAĞIN KENDİ YAZILI ŞARTI KARŞILANDI.**
  # Şart (`features.py`): *«ihraç edilen model geri ithal edildiğinde birebir aynı SQL
  # vermiyorsa bayrak AÇILMAZ»*. Pilot **fikstürde değil canlı katalogda** koşuldu:
  #     dataset **23** · ilişki **31** · ölçü sinonimi **677/677** `ai_context`e taşındı
  #     round-trip kaybı **SIFIR** (ölçü · boyut · zaman boyutu · `base_object`)
  #     `x-dima`: `additive` · `certified` · `origin` · `rol` — dördü de taşınıyor
  #
  # ⚠ Ve pilot bir KUSUR BULDU ve düzeltti: uç `schema()` şeklini besliyordu,
  # `disa_aktar` `packs/` şeklini bekliyordu → canlıda **HTTP 500**. Round-trip kapısı
  # göremedi çünkü fikstürü `packs/` şeklindeydi — *kapı, ucun hiç görmediği bir şekli
  # sınıyordu.* Yeni kapı `schema()`'nın **kendisini** ihraç ediyor
  # (`test_f6_ossie_canli_sekil.py`).
  #
  # 🔴 `on` ŞARTI (gözlenebilir): canlı `GET /connections/{cid}/ossie` **200** dönmeli ve
  # gövdesinde `datasets` ≥ 1 + en az bir `ai_context` bulunmalı.
  # ⟳ `ossie_ithal` **`beta`ya ALINDI (2026-08-12)** — aşağıdaki bloğa bak. Eski not: **`off` KALIYOR**: o bir YAZMA yoludur ve gerçek bir müşteri modeliyle
  # pilotu YAPILMADI. *Ölçülmemiş bir yeteneği, kardeşi ölçüldü diye açmak, ölçümü bir
  # törene çevirir.*
  ossie_ihrac: beta
  # FAZ 4.5 — MCP yüzeyi. `off`: /mcp/* 404 döner, HTTP yolu ETKİLENMEZ.
  mcp_yuzeyi: "off"
  # FAZ 5.1 — 6. konuşma türü ("bunu takip et"). `off`: tür tanınmaz, davranış birebir.
  # 🔴 `F6`/`D1` — **AÇILDI (2026-08-10).** Rapor: *«yeni motor yazılmadı, yalnız
  # ERİŞİM açılıyor»* — `zamanla.olustur` zaten var ve testli (`test_tur_takip.py`).
  # ⊙ Kapalı kalmasının sebebi bir tasarım kararı değil, bir **unutulmuşluktu**:
  # ödenmiş, testli bir yetenek kullanıcıya kapalı duruyordu — `E-1`'in tarifi
  # (*«ödenmiş, testli, kapalı duran bir yetenek harcanmış bir emektir»*).
  # 🔴 `on` ŞARTI: canlı turda bir takip kurulup **tetiklendiği** görülmeli.
  tur_takip: beta
  # FAZ 5.10 — KPI pin. `off`: pano bugünkü davranışında, pinler SİLİNMEZ görünmez olur.
  kpi_pin: "off"
  # FAZ 5.12 — içgörü paketi. `off`: `viz_paketi` None, tekil kart birebir bugünkü hâlde.
  ui_icgoru_paketi: "off"
  # FAZ 5.13 — bilgi merkezi (hayalet seri + kural motoru). `off`: /ask cevabı DEĞİŞMEZ.
  ui_knowledge_center: "off"
  # FAZ 6.0/6.1 — onay akışı + D9. `off`: bugünkü "her yazmaya istem" davranışı birebir.
  onay_akisi: "off"
  # FAZ 6.5 — dış geliştirici API'si. `off`: dış uç yok.
  public_api: "off"
  # FAZ 6.6 — kanal kimlik eşlemesi. `off`: bildirimler bugünkü adresleme ile gider.
  kanal_kimlik: "off"
  # 🔴 FAZ 6.5 — gömülü pano. **AÇILAMAZ**: `motor_cls=off` (motor-RLS hiç açık değil) ve
  # yol haritası `always_filter`ı tek başına yeterli SAYMIYOR. Ölçüm `app/embed_kapsam.py::
  # p0_engeli()`de bir KAPI olarak duruyor — belgeye yazıp koda yazmamak, ilan edip
  # uygulamamaktır.
  embed: "off"
  # FAZ 5.14 — Hızlı ↔ Derin. `off`: `mod` alanı yok sayılır, bugünkü davranış birebir.
  hizli_derin: beta   # 🔴 G0.14 — ÖLÜ KONTROL CANLANDIRILDI (2026-08-07).
  #   Ölçüldü: UI'da iki konumlu anahtar VARDI (`ChatPanel.tsx`), kullanıcı tıklıyordu,
  #   sunucu alanı YOK SAYIYORDU (`ask.py:2563`) — çalışıyormuş gibi görünen ölü kontrol.
  #   Açmak SIFIR yeni davranış getirir: `hizli` ≡ `yol_siniri="deterministik"`,
  #   yani zaten kurulu ve testli bir kapıya **ikinci bir ad**. Kod da bunu yazıyor:
  #   *"Aynı davranışa iki ad vermek meşrudur; iki UYGULAMA vermek değildir."*
  #   Alternatif (UI'dan kaldırmak) reddedildi: kontrol yanlış değil, yalnız kapalıydı.
  # 🔴 FAZ 4.3 borcu — takipte çıplak ikinci ölçü adı. **AÇILDI (beta)** — ve açış
  # ölçüme dayanıyor, iyimserliğe değil:
  #   · çok-turlu düşüş  −%18,2 → **−%4,5** (hedef −%10) · karar `kaldi` → **`gecti`**
  #   · korpus doğru-cube **%93,1 → %93,1** (birebir, A/B ölçüldü)
  # Denetimin §4/risk-1 korkusu ("boyut sanılan kelimeler ölçüye çekilir") ÖLÇÜLDÜ ve
  # gerçekleşmedi. ⚠ `beta`: fabrika varsayılanı açık ama tenant/rol override'ı hâlâ
  # mümkün — geri alma tek satır.
  olcu_ekleme_takibi: beta
  # FAZ 5.2 — 7. konuşma türü ("paylaş"). `off`: kalıplar eşleşmez, TUR_ANLAT birebir.
  # 🔴 `F6`/`D1` — **AÇILDI (2026-08-10).** Raporun kendi cümlesi: *«çalışan, testli
  # bir yetenek BİR KELİME yüzünden kullanıcıya kapalıydı»*.
  # 🔴 `on` ŞARTI: paylaşım bağlantısının **yetki sınırını** koruduğu canlı turda
  # doğrulanmalı — paylaşım bir RLS gevşemesi DEĞİLDİR.
  tur_paylas: beta
  # ⚠ AŞAĞIDAKİ ÜÇÜ FAZ 4.6'DA EKLENDİ — bir denetim ölçtü: `FLAG_REGISTRY`'de kayıtlı
  # ama bu dosyada YOKLARDI, yani varsayılanları hiçbir yerde YAZILI DEĞİLDİ. Yokluk ile
  # `"off"` çözümlemede aynı sonucu verir (`resolve_for` `off`u kümeden düşürür), bu
  # yüzden ekleme DAVRANIŞ DEĞİŞTİRMEZ — ama *"bu bayrak var ve kapalı"* ile *"böyle bir
  # bayrak yok"* okuyucu için aynı şey değildir. 🔴 Yazılı olmayan bir varsayılan,
  # tartışılamayan bir varsayılandır.
  ask_async_discovery: "off"   # 🔴 G5.10 — AÇILDI, ÖLÇÜLDÜ, GERİ ALINDI.
  # ⊙ A/B (aynı kod, yalnız bu bayrak): açıkken `tests/test_telemetri_yazma_yolu.py`
  # **2 kırmızı**, kapalıyken **12 yeşil**. Kayıp gerçek ve dar: LLM yoluna düşen soru
  # telemetriye **red gerekçesi OLMADAN** yazılıyor — çünkü `_teshis` şemayı arka-plan
  # thread'inde okuyamıyor (istek nesnesi cevap gönderildikten sonra kullanılamaz).
  # 🔴 O kolonun tek varlık sebebi *"kapsam boşluğunun EN BÜYÜK kümesi"*ni saymaktı.
  # ⚠ Yani hız kazancı gerçek ama bedeli bir **ölçüm kanalı**: bu turda tam bu sınıftan
  # bir kusurun (`nl_corpus`'un süreç yarısı) faturası ödendi.
  # *Bir taşımayı değiştirmek ölçümü de değiştirir; ölçüm taşımadan bağımsız olmalıdır,
  # yoksa her hızlanma bir körleşmedir.*
  # ÖN KOŞUL: teşhis kuyruğa girmeden ÖNCE (istek thread'inde) hesaplanıp işe taşınmalı.   # 🔴 G5.10 — AÇILDI. Discovery arka-plan işi + canlı akış.
  # ⊙ Ölçülen kazanç: deterministik yol ~100–800 ms (akış gereksiz), **LLM yolu 2,8–5,5 sn**
  # ve o süre bugün SESSİZ geçiyor. `AskJob.trace` aşamaları ZATEN taşıyordu; eksik olan
  # onları **anında** iletmekti — `GET /ask/jobs/{id}/stream` ve ön yüz tüketicisi
  # (`api-client.ts::streamAskJob` + `DurdurDugmesi`) **ikisi de yazılıydı ve kapalıydı**.
  # ⚠ Bayrak neden bugüne kadar kapalıydı: `/ask`'in dönüş ŞEKLİNİ değiştiriyor. Artık
  # kapalı değil çünkü tüketicisi tam: `job_id` → akış → `tamam` olayı → aynı `AskResponse`.
  # 🔴 ANLATI AKIŞI BU BAYRAĞA DAHİL DEĞİL — sebebi ölçüldü, `Ö1` bölümüne bak.
  cekirdek_katman: "off"       # FAZ 2.1 — çekirdek katman derlemeye girer
  threaded_chat: "off"         # thread'li sohbet UI'ı
  # FAZ 2.5 — hedef kıyası. `off`: `resp.hedef` HİÇ üretilmez → yanıt bayt bayt bugünküyle
  # aynı (KURAL B). 🔴 Açıkken bile hedef UYDURULMAZ: `target:` beyanı yoksa `None`.
  # 🔴 `F6`/`D1` — **AÇILDI (2026-08-10) ve açmanın bedeli KANITLANDI, tahmin edilmedi.**
  #
  # ⊙ Ölçüldü: `hedefler:` beyan eden küp sayısı **0**. `hedef.blok()` beyan yoksa
  # `None` döner → `resp.hedef` üretilmez → yanıt **bayt bayt bugünküyle aynı**.
  # Yani bu açılış bugün **sıfır davranış** değiştirir; ilk `hedefler:` beyanıyla
  # birlikte canlanır.
  #
  # ⚠ Rapor `F6` şartı *"önce KAPI, sonra aç"*: `tests/test_hedef_kiyasi.py` (9 kapı)
  # yazıldı. Sözleşmenin üç sert kuralı kilitli: beyan yoksa **uydurma yok** ·
  # `None ≠ 0` (*sıfır hedef ULAŞILMIŞ bir hedeftir, hedefsizlik ölçülemezliktir*) ·
  # `gerceklesen` **sonuçtan** okunur.
  #
  # 🔴 **`on` ŞARTI (`E-4`):** en az bir küp `hedefler:` beyan etmiş **ve** canlı bir
  # turda blok doğru görünmüş olmalı (`ulasildi` yönü `lower_is_better` ile tutarlı).
  # ⚠ Ön koşulu `B-5`'tir: yön beyanı yanlışsa *«hedefe ulaşıldı»* kararı TERS çıkar.
  hedef_kiyasi: beta
  cikti_yorumlama: beta   # evrensel çıktı yorumu — her tablo/grafik/rapor/KPI
  sql_display: beta       # üretilen SQL'i kullanıcıya göster ('+ sql göster')
  verify_button: beta     # ✓/✗ doğrulama — VQR öğrenme (eskiden boyahane sektöründe)
  scheduled_reports: beta # 🔔 zamanla + bildirim (ADR-0011; eskiden boyahane sektöründe)
  dashboards: beta        # kullanıcı panoları — canlı izleme (§9)
  next_steps: beta        # rehberli analitik K2 — sonraki adım (kırılım/ölçek/zaman) chip'leri
  ask_intent_first: beta  # /ask: route() boşsa LLM'e Intent-JSON doldurttur (Faz 1 canary —
                          # route()'un kendisi bu bayraktan bağımsız her zaman çalışır)
  # 🔴 `§B2` (rapor §14 FAZ 1) — garsona doğrulanmış örnek göster. `few_shot_block` zaten
  # yazılıydı ama YALNIZ Discovery'ye (%1,7) bağlıydı; garson trafiğin %37'sini taşıyor.
  # Dış ölçüm: Cube +17…+23 puan, 4 KB'lık bir bağlamdan. Kapı garsonda daha sıkı:
  # yalnız insan onaylı kayıtlar (`user`/`user_verified`/`chip_approved`).
  # ⚠ Risk: bağlam kirlenmesi (Hex: «çelişkili bağlam modeli çöküş moduna soktu») → k=3.
  vqr_few_shot: beta
  # §D3 — CEVAP BİÇİMİ BİR KARARDIR. Öneri şeridinin kova kotaları soru türünden
  # gelir (`app/bicim.py`). Ölçüldü (§18'in sekiz sorusu, 2026-08-11): chip dizisi
  # 6,6,6,6,5,6,6,5 — soru ne olursa olsun aynı boyda şerit. Ve asıl kusur sayı
  # değil ALÂKASIZ KOVA: bir «ciro» sorusuna «+ fire» önerilmesi.
  # Karar deterministik ve tablolu (ADR-0024'ün ilkesi); LLM seçmez.
  bicim_karari: beta
  # §B1 — ŞEMA DARALTMA. Garsona her soruda 23 küp / 23.729 karakter gidiyordu.
  # ⚠ Raporun kartı «varsayılan off» diyor; burada BİLİNÇLİ olarak `beta`:
  # sekiz garson sorusunda «budanmış küme garsonun GERÇEKTEN seçtiği küpü içeriyor
  # mu» sınandı → 8/8 kapsandı, 0 kayıp. Fail-open bir SAYIYA değil KANITA bağlı
  # (sorunun her içerik sözcüğü katalogda açıklanabiliyor mu).
  # 🔴 Geniş yayılım için ön koşul A1 (garson korpusu): kapı route()'u ölçer,
  # garsonu ölçmez — bu bayrağın gerilemesi kapıda GÖRÜNMEZ.
  sema_daraltma: beta
  # §B4 — PLAN ONARIM DÖNGÜSÜ. Ölçüldü (/stats/plan, 2026-08-11):
  #   denendi=16 · onarildi=1 · dustu=3 → onarim_tutma_yuzde=25
  #   red_nedenleri={"ulasilmaz": 4}  ← dört reddin DÖRDÜ de aynı sınıf
  # Tek tur 4'te 1 tutuyordu ve istem yalnız kusuru söyleyip ÇAREYİ söylemiyordu.
  # Tavan 2 (Magentic-One stall≤2) + sınıfa özgü yönerge. Kapalıyken tavan 1.
  onarim_dongusu: beta
  agent_plan_secimi: "off"  # FAZ 4: çapraz-alan pilotu — Planlayici.sec() planı ÖNERİR,
                          # dört kapı DENETLER, agent_run makbuzu kaydeder. Seçim ≠
                          # çalıştırma. `off` — sıcak yola LLM çağrısı ekliyor.
  prompt_enhancer: "off"  # FAZ 3b: route() BOŞ dönerse soruyu katalog terimleriyle
                          # YENİDEN YAZ ve route()'u TEKRAR dene. LLM yapı seçmez, yalnız
                          # metni iyileştirir. Planlayıcının DÖRT KAPISINDAN geçer
                          # (route denenmeden seçilemez). `off` — sıcak yola LLM çağrısı
                          # ekliyor; açılması bilinçli karar olmalı (KURAL B, testli).
  # 🔴 `G2` — DİYALOG BELLEĞİ (`DA-5`). Bayrak, katman indiğinde **hiç yaratılmamıştı**;
  #   GERİ AL sözleşmesi (*"`off` → davranış birebir bugünkü"*) uygulanamaz durumdaydı ve
  #   bunu bir denetim ajanı buldu. `MIMARI §9.11`: *bir kill-switch yalnız KODDA varsa
  #   yarımdır.*
  #   Kapalıyken `AskRequest.diyalog_durumu` sunucuya **hiç girmez** → `KURAL_DEVAM`
  #   ateşlenmez → *"sorduğunu hatırlama"* davranışı `G2` öncesine döner.
  #   ⚠ `on`: katman canlı kapıda üç koşum yeşil verdi (`3·süreklilik` 3/3, `9·onarım` 1/1)
  #   ve istemci yankısı `Z` turunda bağlandı; kapalı tutmak ölçülmüş bir kazancı
  #   saklamak olurdu.
  #   ⚠ **`on` YAZILAMAZ:** YAML `on/off/yes/no`'yu **BOOLEAN** okur ve `resolve_for` onu
  #   elemez → bayrak sessizce açık kalır (kapı: `test_YAML_off_TUZAGI_kapali`). Geçerli
  #   aşamalar: `off` · `alpha` · `beta` · `prod`.
  diyalog_bellegi: prod
  # 🔴 `M-4` — DÖNEM BELİRTİLMEDİYSE **BEYANLI VARSAYIM** (verinin son 12 ayı).
  #   Ölçülen kusur: korpusun **%13,7'si** `CLARIFY:dönem`; P/R turlarında **on**
  #   senaryoda ölçü·kırılım·sıralama çözülmüşken tek eksik dönem yüzünden cevap
  #   tutuldu. Mekanizma `donem_capasi.varsayilan_yerinde` (gerekçe orada).
  #   ⚠ `off` bilinçli ve `KURAL B` gereği: bu bir **davranış değişikliğidir** —
  #   "sormak" yerine "varsayıp söylemek". Kazanç (kapsam) ile bedel (`sessiz_yanlis`)
  #   ölçülmeden `alpha` olmaz; doğruluk vetosu kutsaldır.
  #   ⚠ Varsayım **kataloğa değil VERİYE** bağlı: bayat bir beyan yanlış pencere üretir,
  #   veri kendini günceller. Ölçülemezse hiçbir şey yapılmaz (fail-closed) ve
  #   netleştirme aynen çalışır — kaldırılmadı, ikinci seçenek oldu.
  varsayilan_donem: beta   # ⏳ `D3` ÖLÇÜM KOŞUMU (2026-08-10) — karar korpustan sonra   # ⚠ TIRNAK ŞART: YAML 1.1 çıplak `off`u BOOLEAN okur
  t2_anlatici: alpha   # 🔴 G5 — AÇILDI (2026-08-07). Kapalılığın sebebi bir KARAR
  #   değil, ÖLÇÜLMEMİŞLİKTİ: MIMARI §6.19z'de kazanç ölçümü "⊘ ÖLÇÜLEMEDİ — kota".
  #   Üç ön koşul da sağlandı ve üçü de FAIL-CLOSED:
  #     G0b · hava boşluğu   → gerçek değer/sayı yer tutucuya çevrilir, DIŞARI ÇIKMAZ
  #     G4  · iddia kapısı   → "tedarikçi kırılımı da ekleyebilirim" DÜŞER
  #     G5.1· ön koşul kilidi→ iddia.py yoksa anlatı HİÇ üretilmez
  #   ⚠ `alpha` bilinçli: KURAL G-1 gereği iki koşum ölçülmeden `beta` olmaz.
  #   Kazanç yoksa `off`'a döner VE gerekçesi MIMARI'ye yazılır (§13.4).
                          # Türkçeye çevirir. LLM ÜSLUBU yazar, SAYIYI sistem koyar.
                          # narration_guard ZORUNLU çıkış kapısı (fail-closed: eşleşmeyen
                          # sayı taşıyan cümle YAYIMLANMAZ). `off` — sıcak yola LLM çağrısı
                          # ekliyor; açılması bilinçli bir karar olmalı (KURAL B, testli).
  t2_sablon: beta  # 🔴 T2'nin DETERMİNİSTİK ilk basamağı — *"basit olanı sistem yazsın"*.
  # ⚠ Sayıya dokunmaz: cümleyi `interpret()`'in KENDİ metinlerinden kurar, yani guard'dan
  # geçmesi tesadüf değil yapısal. Tanımadığı olgu türünü görünce turu LLM'e DEVREDER —
  # görmezden gelip kalanı anlatmak, *eksik ama tam görünen* bir özet vermek olurdu.
  # 🔴 `§75` — SALINIM YÖNETİMİ. Üç sondajla ölçüldü: aynı soru aynı dakikada bazen tam
  # cevap bazen netleştirme veriyor; `k=3` + `2/3` pratikte bir OY BİRLİĞİ şartıdır.
  # Açıkken uyuşmazlıkta ÇOĞUNLUK adayıyla cevaplanır, uyum oranı ize yazılır ve
  # `uyum.py` eksikleri ayrıca beyan eder. Kapalı doğar: takas korpusla ölçülecek.
  oylama_cogunluk: "off"
  # 🔴🔴 `§V2` — oy ÇEKİRDEĞE verilir, zenginlik BİRLEŞTİRİLİR. Ölçüldü (V turu, on
  # Intent turu): **kazanan 1 oy → 7 kez**. Sebep kanonik anahtarın **tam `cq`** olması:
  # `order` yazmayan iki oy bedavaya uyuşur, yazan tek oy yalnız kalır — zengin cevap
  # kendi zenginliği yüzünden oy kaybeder. Canlı bedeli `V13` (11 satır) ve `V14`.
  # ⚠ Açık doğar: kusur ölçüldü, takas **tek yönlü** (bir alan ya taşınır ya düşer;
  # çelişkide düşer — yani en kötü durum bugünkü davranış).
  oylama_cekirdek: beta
  # 🔴🔴 `D2`/`F2` — **AÇILDI (2026-08-10).** Kapattığı şey bir kusur değil bir **YALAN**.
  #
  # ⊙ Aritmetik kanıtlandı (`test_oylama_paydasi.py`): üç çağrının biri cevap, ikisi
  # `{"cube": null}` (*«bu soru tek cube ile yanıtlanamaz»*) dediğinde —
  #   payda `cands` (bugün) → `1/1` = **%100** *«oy birliği»* 🔴
  #   payda `oylar` (bayrak) → `1/3` = **%33**, `2/3` eşiğinin altında → **netleştirme** ✅
  #
  # 🔴 Yani **şüphenin en yüksek olduğu durum, sistemin en emin göründüğü durumdu.**
  # Çekimser bir **bilgidir**, gürültü değil; onu paydadan düşürmek **hayır oylarını
  # saymadan oy birliği ilan etmektir**. `MIMARI`: *«kalibre edilmediği sürece o sayı
  # bir güven değil bir SÜSTÜR.»*
  #
  # ⚠ **Neden korpus kanıtı beklenmedi (`E-2`):** `nl_corpus` tanımı gereği `rule`
  # sağlayıcıyla koşar — oylama yolu orada **hiç çalışmaz**. Korpus kanıtı beklemek,
  # **hiç gelmeyecek bir ölçümü beklemek** olurdu; bu bayrağın enstrümanı `A2` kasetli
  # garson korpusudur. *Bir mekanizmayı, koşmadığı bir korpusla yargılamak, sessizliği
  # bir cevap sanmaktır.*
  #
  # ⊙ Ve `E-3` kararı nettir: kapsam düşüp `sessiz_yanlis` düşüyorsa bu bir **kazançtır**
  # — *bir soruyu cevaplamamak, yanlış cevaplamaktan iyidir.* Kullanıcının merdiveni de
  # aynısını söylüyor: **%100 emin değilse çekil**; bu bayrak o kuralı **oy düzeyine**
  # taşır.
  #
  # 🔴 `on` ŞARTI (`E-4`): `A2` kasetiyle A/B — `sessiz_yanlis` **artmamalı** ve
  # netleştirmeye düşen vakaların **kaçının** gerçekten belirsiz olduğu sayılmalı.
  # ⚠ Kaset bunu **sıfır API** ile ölçebilir: kayıtlı oylar aynı kalır, yalnız **payda**
  # değişir — bir bayrak A/B'si için ideal enstrüman.
  oylama_paydasi: beta  # 🔴 Kusur GERÇEK, takas ÖLÇÜLEMEZ — bu yüzden kapalı.
  # ⊙ Doğrulandı: `agreement = len(best)/len(cands)` ve `cands` `{cube:null}` oylarını
  # eliyor → 1 cevap + 2 "bilmiyorum" = uyum **1,0**. Şüphe en yüksekken sistem en emin.
  # ⚠ Açmanın bedeli kapsam: eşik (`>= 2/3`) aynı orandan geçiyor. Yön doğru (tahmin
  # yerine soru) ama bedel bu koşumda ÖLÇÜLEMEZ: `nl_corpus` `rule` ile koşar,
  # `eval --slice llm` 4 vaka. *Ölçülemeyen bir takası varsayılan yapmak, kullanıcı
  # adına karar vermektir.* ÖN KOŞUL: `eval --slice llm` büyüsün.
  katalog_sozlugu: beta  # 🔴 LLM artık Türkçe eşanlamları GÖRÜYOR.
  # ⊙ Ölçüldü: 23 cube'un 23'ünde `synonyms`+`measure_synonyms` beyan edilmiş, katalogda
  # `verim`/`randiman`/`verimlilik`/`hasılat`/`bordro` **0 kez** geçiyordu. Canlı sonuç:
  # *"verimlilik"* sorusunda **9/9 Intent çağrısı `{"cube":null}`** — aday YOKLUĞU.
  # ⚠ `ADR-0008` ihlali DEĞİL: yeni liste yazılmıyor, var olan beyan ikinci tüketiciye
  # gösteriliyor. Kapalıyken katalog metni **bayt bayt bugünkü**.
  varlik_perdesi: beta  # 🔴 G0b.6: hava boşluğu — gerçek değer sağlayıcıya HAM gitmez.
  # ⚠ Bilgi KAYBI değil kazancı: ham değer *"bu ne?"* sorusunu açık bırakır, yer tutucu
  # hangi boyuta ait olduğunu SÖYLER. Kapalıyken soru ham gider (bugünkü davranış).
  referans_dili: beta  # 🔴 G6: kıyasın İKİ UCU + Intent-JSON'da `blend`.
  # ⚠ `beta` — çünkü kazancı ölçülebilir ama ölçülMEDİ: `blend`'in taze soruda kaç `R1`'i
  # kurtardığı **gerçek bir sağlayıcı** ister (`eval --slice llm`, bugün 4 vaka).
  # Kapalıyken: makbuzda kıyas satırı YOK, şemada `blend` YOK → yanıt bayt bayt bugünkü.
  # 🔴 FAZ O-4 — PLANLAYICI İLE GARSON AYNI KİŞİ. Garson bir `CubeQuery` yerine bir PLAN
  # çevirir; tek `SORGU` adımlı plan bugünkü `CubeQuery`'nin ta kendisidir (`tek_adimli`),
  # yani basit soruda çağrı sayısı da çıktı da BİREBİR aynı. Çok adımlı plan bugünkü
  # yolda bir OY DÜŞÜŞÜDÜR — merdiven bugünkü gibi sürer, plan saklanır (E3: orkestratör
  # merdivenin YERİNE değil BOŞLUĞUNA girer).
  # ⚠ "off" — çünkü kazancı ÖLÇÜLMEDİ: kaç sorunun gerçekten çok adımlı olduğu ve plan
  # uzunluğunun gecikmeye ne kattığı (E6/E9) canlı ölçüm ister. KAPALIYKEN `sarmala()`
  # nesnenin kendisini döndürür → davranış bayt bayt bugünkü (KURAL B, testli).
  # 🔴 ÖLÇÜLMÜŞ BİR KUSURUN KARŞI ÖNLEMİ — aynı soru, farklı cevap.
  # Ölçüldü (2026-08-09): ham tek çağrıda **2/8**, üretim yolunda (k=3 oylama) **1/8**
  # farklı `cube_query`. Sapan soruların hepsi **çok sahipli** bir ölçü içeriyordu —
  # 127 ölçü adının **9'u** birden çok küpte tanımlı ve katalog bunu hiç söylemiyordu.
  # Model seçemediği için değil, **belirsiz olduğunu bilmediği** için savruluyordu.
  # ⚠ Blok ölçü adına BİTİŞİK DEĞİL (`§CC-D`: `↓` ada bitişik yazılınca model onu adın
  # parçası sandı ve beyaz liste fire/duruş/maliyet/karbon'u sessizce ulaşılamaz yaptı).
  # ⚠ Blok KARAR VERMEZ: *«burada bir seçim var»* der. Sahiplik bir alan kararıdır
  # (`metrik_kaydi.hakem` + `sahiplenilen_terimler`, bugün boş).
  # 🔴 "off" — **kazancı bu sonda kümesinde GÖSTERİLEMEZ**: denklik ölçümünde plan
  # sapması zaten 0/5 ve tabanın tek sapması (`en yüksek cirolu 5 müşteri`) çok sahipli
  # bir ölçüden değil sıralama/limitten geliyor. *Bir kazancı gösteremeyen bir ölçümle
  # bayrak açmak, ölçmemekle aynı şeydir.* Kendi sonda kümesi yazılınca açılacak.
  katalog_belirsizlik: "off"
  # 🔴🔴 `§DK-2` — SÜZGEÇ DEĞERİ ÇAPASI (2026-08-10, canlı curl turunda ölçüldü).
  #
  # Garson süzgecin **değerini de kendisi yazıyor** ve varlığını hiçbir şey sormuyordu:
  #   «oee düşük olan makinelerin bakım maliyeti» → `makine eq "Bakım"` → **0 satır**,
  #   beyan YOK. Kullanıcı bunu *«bakım maliyeti sıfırmış»* diye okur — bir uydurma sayı
  #   değil bir **uydurma yokluk**, ve yokluğun uydurması daha sinsidir.
  #
  # Kapı üç sonuç üretir ve üçü de BEYANLI: değer listede → dokunulmaz · tek yakın
  # karşılık → düzeltilir **ve söylenir** · karşılık yok → sorgu **koşturulmaz**, gerçek
  # değerler chip olur. Yalnız **tam** enum'larda konuşur (`wren_service` bir boyutun
  # değerlerini ancak sayılabildiğinde yazar) → yanlış-pozitif yapısal olarak dışarıda.
  #
  # ⏳ `beta` — davranış değiştirir, `KURAL B` ile bayraklı. Kapalıyken davranış birebir.
  # ⚠ Bu kapı `§DK`'dan (katalog enum düzeltmesi) ÖNCE açılamazdı: o gün enum'lar yalan
  # söylüyordu (`parti.musteri` → `M1001…`, oysa veri *TOROS ÖRME…* tutuyor) ve kapı
  # DOĞRU sorguları reddederdi. *Bir doğrulayıcı, doğruladığı kaynaktan güvenilir olamaz.*
  deger_capasi: "beta"
  # 🔴🔴 `C1`+`C3` — TERS YÖN ALANI: sistem sözlüğünü **kullanımdan** yazar.
  #
  # Kullanıcı kararı (`E-7`): *«belki bunu sorgu içinde çözeriz, ayrı LLM sorgusu
  # olmayacak — çok vakit ve maliyet kaybı olur.»* Bugünkü `prompt_enhancer` bir
  # **ikinci tur**dur; bu alan **0 ek tur** ister: Intent çağrısı kataloğu zaten görüyor
  # ve model `zayiat → toplam_fire_kg` eşlemesini **zaten yapıyor** — ondan istenen tek
  # şey onu **söylemesi**.
  #
  # Biçim bilinçli (`C1`, kullanıcı: *«katalogdaki kelimelerden birinin eş anlamlısı var
  # mı diye bakacak — nokta atışı»*): çıktı serbest metin değil **katalogdan bir ad**;
  # `eslesen_terim_oku` onu beyaz listede sınar → model ad **uyduramaz, seçer**.
  #
  # ⚠ `cube_query`'nin parçası DEĞİL: alt çizgili taşıyıcı olarak okunur, `_canon_cq`
  # onu kimlikten saymaz (oy bölünmez), huni boşaltır, derleyiciye gitmez.
  # ⚠ `B-8`'in sert sınırı: eşleme **izde görünür** — sessiz bir öğrenme bir sızıntıdır.
  # ⚠ Belirsizlik burada ÇÖZÜLMEZ: `bakiye` gibi ≥2 sahipli terim kuyruğa DÜŞMEZ.
  ters_yon_alani: "beta"
  # 🔴🔴 **AÇILDI (2026-08-09) — ve gerekçesi bir umut değil bir SAYI.**
  #
  # ⟳ Bir kez kapalı bırakılmıştı: `EE` turunun A/B'si arıza oranını %55 → **%65**
  # ölçmüştü. O ölçüm **yarım bir göçü** ölçüyordu — plan ve `select_cube` bir aradaydı
  # ve `_select_consistent`'ın oyu **iki farklı dağılımdan** besleniyordu. Yani `-10`
  # puan planları değil **birlikteliği** yargıladı.
  #
  # Tam göç (`O-14`) birlikteliği kaldırdı: `PlanGarsonu`'da `select_cube` **yedeği
  # yok**, üç oy da tek kaynaktan. Ve denklik kapısı (`O-13`, `lab/plan_denklik.py`)
  # simetrik ölçtü — iki taraf da üretim yolundan (k=3 oylama):
  #
  #     PLAN  sapması : 0/5        ← beş sorunun beşinde cube_query BİREBİR aynı
  #     TABAN sapması : 1/5        ← bugünkü yol kendisiyle bile anlaşmıyor
  #     ölçüt `plan ≤ taban`  →  ✅ GEÇTİ
  #
  # ⊙ Yani plan yolu bugünkü yoldan **daha kararlı**. Ve `route → garson` karar yüzeyi
  # **dokunulmadı**: değişen şey hangi yola gidildiği değil, garsonun **çıktı biçimi**.
  # LLM çağrı sayısı da değişmiyor — garson zaten yalnız `route_hit is None` dalında
  # çağrılıyor.
  #
  # ⚠ `beta`: fabrika varsayılanı açık, tenant/rol override'ı hâlâ mümkün. Geri alma
  # tek satır. 🔴 Ön koşulları kapalı: `O-10` (üç ölü fiil) · `O-11` (istem sözleşmesi)
  # · `O-12` (sayaçlar) · `O-13` (denklik). Bunlar olmadan bayrak açılsaydı plan kurulur
  # ama **koşamazdı**.
  orkestrator_plan: beta
  llm_sema_kisitli: beta  # FAZ 3a: Intent-JSON'u sağlayıcının NATIVE tool-use'una taşı —
                          # cube/ölçü/boyut adları o anki kataloğun ENUM'u olarak şemaya
                          # gömülür (enum CUBE'A GÖRE daralır; `cube:null` reddetme dalı
                          # KORUNUR). Desteklenmezse serbest-JSON yedeğine düşer.
                          # KAPALIYKEN davranış bugünküyle BİREBİR aynı (KURAL B, testli).
  # FAZ 0.18 — METRİK KAYDI = HAKEM. Kapalıyken kayıt ŞEMAYA HİÇ YAZILMAZ ve
  # `cube_router._match_cube` onu görmez → davranış BİREBİR bugünkü (GERİ AL).
  # Açıldığında bile taslak kayıt `sahiplenilen_terimler: []` ile gelir: yani "bu terim
  # çakışıyor" der, "sahibi şudur" DEMEZ. Sahiplik kararı FAZ 3.1'in turudur.
  # FAZ 0.5 — ÇAPA ZİNCİRİ. Kapalıyken `capalar` listesi BOŞ kalır → `context.coz()`
  # bugünkü yapısal/ham/taze dallarını kullanır → davranış BİREBİR bugünkü (GERİ AL).
  # FAZ 0.13 — SOSYAL SINIF. ⚠ Bayrak bu maddede EKLENDİ: teslim borcunun asıl parçası
  # buydu (MIMARI §6.13z/9.11: "bir kill-switch yalnız KOD'da varsa YARIMDIR" — sınıf
  # kod olarak vardı, geri alma yolu YOKTU). Varsayılan `on`: sınıf ölçülmüş ve
  # kanıtlanmış bir kusuru kapatıyor (`teşekkürler` → uydurma SQL); kapatma yolu artık
  # VAR ama gerekçesiz kapatılmaz.
  sosyal_sinif: prod
  # 🔴 **AÇILDI (beta)** — denetim raporunun §6 üçlüsünün son ayağı. Kapalıyken `_capalar`
  # hep boş kalıyordu, yani `KURAL_CAPA` (*"karta yanıt"*) **üretimde hiç ateşlemiyordu**:
  # mekanizma vardı, **yolu kapalıydı**.
  #
  # Açış ölçüme dayanıyor (§4/risk-2: *"çapa zincirini açmak YOL DEĞİŞTİRİR"*):
  #   · konuşma senaryoları **dokuz sınıf tabanda** (bayrak açıkken koşuldu)
  #   · `test_capa_zinciri` + `test_baglam_cozucu` + `test_atif_baglami` **52 yeşil**
  #   · kural gerçekten ateşliyor: tek çapa → `capa:karta-yanit`; farklı cube'lar →
  #     `capa:coklu-celiski` ve **SORUYOR** (ADR-0008: belirsizlikte tahmin etme)
  #
  # ⚠ Etki **sınırlı ve kasıtlı**: yalnız istemcinin `reply_to_cube_query` gönderdiği
  # turlar — yani kullanıcının **bilerek bir karta yanıt verdiği** turlar.
  capa_zinciri: beta
  # FAZ 3.1b — SAHİPLİK TURU + TENANT KAPSAMI. Karar kaydı
  # (`packs/cekirdek/sahiplik_kararlari.yml`) artık yalnız **ÖNERİR**, uygulamaz.
  #
  # 🔴 Neden: FAZ 3.1'de pack kararları doğrudan uygulanıyordu ve korpus GERİLEDİ —
  #     TOPLAM %93,2 → %92,6 · gitas erişim %72 → %69
  # Teşhis: kararlar boyahane/atiksan'ın ÖLÇÜLEN kusurları için yazılmıştı ama pack
  # düzeyinde HER ŞİRKETE dayatılıyordu. Bir tenant'ın alan bilgisini bütün tenant'lara
  # dayatmak, alan bilgisi olmaktan çıkıp VARSAYIM olur.
  #
  # Şimdi: pack ÖNERİR (`onerilen_sahip`), UYGULAYAN tek şey tenant'ın kendi kararıdır
  # (`MetrikSahipligi`, FAZ 2.2b) — öneri ekranda görünür, tek tıkla kabul edilir.
  # Yani alan bilgisi KAYBOLMAZ, yalnız DAYATILMAZ. Bayrak açık ama tenant kararı yoksa
  # davranış BİREBİR bugünkü — korpusla doğrulandı.
  metrik_kaydi: beta
  # 🔴🔴 `D5` — **NEDEN KAPALI OLDUĞU BUGÜN ARAŞTIRILDI VE ÖLÇÜLDÜ (2026-08-10).**
  #
  # Rapor `F5`: *"8 test dosyası · 7 kod dosyası — ve hâlâ `off`. Neden kapalı olduğu
  # HİÇBİR YERDE yazılı değil. İlk iş gerekçeyi bulmak; gerekçe yoksa bugün açılabilir."*
  # Gerekçe **vardı ama yazılmamıştı**; işte:
  #
  # ⊙ ÖLÇÜM (canlı control-plane): `DbConnection` **0 satır** · `SyncState` **0 satır**.
  # Tazelik `SyncState.last_synced_at`'ten okunur (`answer.py::_tazelik_blogu`).
  # Kayıt yoksa kademe **`bilinmiyor`** olur ve o kademede — tasarım gereği —
  # **SAYI GÖSTERİLMEZ**. Yani bu bayrak bugün açılsaydı ürün **hiçbir sayı
  # göstermezdi**. Bu bir gerileme değil, bir **karartma** olurdu.
  #
  # 🔴 VE ASIL KUSUR BURADA: bayrak *«senkron bilinmiyor»* ile *«veri bayat»*ı **aynı
  # kademeye** koyuyor. Dosya-tabanlı (DuckDB) bir kiracıda senkron diye bir kavram
  # YOKTUR — bilinmemesi bir bayatlık işareti değil, **sorunun geçersizliğidir**.
  # `B4` (*"bilinmeyen tazelik taze DEĞİLDİR"*) doğru bir kuraldır ama **bağlanabilir**
  # kiracılar için yazılmıştır; dosya kiracısına uygulanınca *ölçülemeyeni kötü
  # varsaymaya* dönüşüyor — ki o da bir uydurmadır, ters yönde.
  #
  # 🔴 `on` ŞARTI (`E-4`): (1) kiracının **gerçek bir bağlantısı** olmalı
  # (`DbConnection` + `SyncState` dolu), **ya da** (2) dosya kiracıları için `bilinmiyor`
  # kademesi *«sayıyı gizle»* yerine *«tazelik kavramı yok»* diye ayrılmalı.
  # ⚠ İkincisi bir kod işidir ve `F5`'in gerçek içeriği odur — bayrağı çevirmek değil.
  #
  # *Bir yeteneğin kapalı olması bazen bir unutulmuşluk, bazen tek yazılmamış bir
  # gerekçedir; ikisini ayırmanın tek yolu aramaktır.*
  tazelik: "off"          # FAZ 1.7: "bu sayı ne kadar eski?" — dört kademe.
                          # 🔴 `hata` VE `bilinmiyor` kademelerinde SAYI GÖSTERİLMEZ.
                          # Kapalıyken alanlar `None` → davranış birebir bugünkü.
                          # ⚠ Açmadan önce: demo/DuckDB tenant'larında `SyncState` YOK,
                          # yani hepsi `bilinmiyor` → sayı gizlenir. Açılması bir ÜRÜN
                          # kararıdır ve tenant başına ölçülmelidir.
  metrik_sertifikasi: "off"  # FAZ 1.5: metriğin tanımını KİM onayladı + o onaydan beri
                          # tanım/köken DEĞİŞTİ Mİ. Kapalıyken rozet gösterilmez ama
                          # ⚠ TANIM-HASH ÇÜRÜMESİ KAPALIYKEN DE İŞLER — aksi hâlde
                          # bayrak açıldığında BAYAT sertifikalar "geçerli" görünürdü.
  lineage: "off"          # FAZ 1.6: KOLON düzeyi köken — "bu sayı hangi tablonun hangi
                          # kolonundan, hangi DÖNÜŞÜMLE geldi?" `answer.koken()` İLİŞKİ
                          # düzeyindedir (hangi kırılım hangi join'den); bu ondan FARKLI
                          # bir sorudur ve yerine geçmez.
                          # KAPALIYKEN makbuz bugünküyle BİREBİR aynı (KURAL B, testli).
                          # Toplanan geçmiş SİLİNMEZ (ADR-0019).
  netlestirme_onceligi: "off"  # FAZ F: katalogda ≥2 SAHİP olan bir ölçüde (ör. `bakiye`
                               # → cari|mizan) netleştirme chip'i Intent-JSON'u ÖNCELER.
                               # ⟳ FAZ X (3 Ağustos 2026): KARAR ARTIK ÖLÇÜLMÜŞTÜR —
                               # "kota doldu" gerekçesi kalktı, yerine SAYI geldi.
                               #
                               #   A/B (63 etiketli vaka, CANLI): bozulan 0 · kurtarılan 0
                               #   Kurtarma (15 gerçek ifade, CANLI): cevaplanan 12 → 8
                               #     KURTARILAN 1 (doğru ölçüyle 0) · KAYBEDİLEN 5
                               #   Kararlılık (aynı soru ×3, CANLI): 5/6 KARARLI
                               #     'bu yıl borçları' TEK kararsız (cari×2, mizan×1)
                               #
                               # Yani kaybedilecek 5 cevap YAZI-TURA DEĞİL: model tutarlı
                               # ve savunulabilir seçiyor (`cari` = müşteri defteri).
                               # Kabul ölçütü (B2: doğru kurtarma > 0) KARŞILANMIYOR ve
                               # kayıp gerçek → KAPALI KALIYOR.
                               #
                               # 🔴 KARARIN EVİ ARTIK `MIMARI.md` §7 — "FAZ 0.4 (v1) ·
                               # netlestirme_onceligi". Bir YAML yorumu mimari otorite
                               # DEĞİLDİR ve yol haritasının KAPI'sı birebir "nedeni
                               # MIMARI.md'ye yazılır" diyordu; buradaki sayılar orada
                               # ölçüt+damgayla duruyor ve `test_faz0_4_netlestirme.py`
                               # bu iki yerin AYRIŞMASINI kapıya çevirdi.
                               #
                               # ⚠ Ölçüm sırasında AYRI bir bulgu: "bu yıl bakiye" KARARLI
                               # şekilde `mizan.bakiye` seçiyor ve cevap ₺0 — mizan
                               # yapısı gereği sıfıra denkleşir. Bu bir MOTOR kusuru değil
                               # KATALOG kararıdır (bare `bakiye` hangi cube'un?) ve
                               # katalog sahibine aittir; sessizce yamalanmadı.
  liste_niyeti: beta      # FAZ 2a-5: "listele/detay/dökümü" niyetini bir CUBE KIRILIMINA
                          # çevir (R2 dalı). YALNIZ gerçek bir boyut eşleştiğinde — yoksa
                          # döküm isteyene DEJENERE TEK TOPLAM dönerdi (sessiz-yanlış).
                          # KAPALIYKEN davranış bugünküyle BİREBİR aynı (KURAL B, testli).
  adhoc_cube: beta        # FAZ 1 / K1: Discovery sonucundan oturum-scoped ad-hoc cube türet —
                          # chip/kırılım/drill/doğru grafik Discovery cevabında da açılsın.
                          # YAPI ≠ GÜVEN: source `llm:*` KALIR, confidence None KALIR.
                          # KAPALIYKEN davranış bugünküyle BİREBİR aynı (KURAL B, testli).
  ui_kanit_gorunurlugu: beta   # FAZ 7.8/K1: makbuz ÜÇ KATMAN, tek kapsayıcı. Ölçüldü:
                          # cevap kartında `<details>` SIFIRDI — D3 yanlış kapatılmıştı.
                          # Varsayılan KISA (Steyvers 2025: uzun açıklama doğruluğu
                          # artırmadan güveni artırır). Ayrıntı SİLİNMEZ, katlanır.
                          # KAPALIYKEN eski üç-yüzeyli davranış birebir (KURAL B).
  ui_dcm_modu: "off"      # FAZ 7.11: Deterministik Cevap Modu (banka/kamu satış argümanı).
                          # VARSAYILAN KAPALI ve bu bir karar: DCM bir KISITLAMADIR;
                          # istemeden açılırsa kullanıcı ürünü BOZUK sanır ("neden yazamıyorum?").
                          # Açıkken serbest metin YOK, cevap 3 tıkta ve LLM'siz.
